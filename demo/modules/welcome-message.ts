/**
 * 콘솔 환영 메시지
 */

export function printWelcomeMessage(): void {
  console.log(`
  SEO Toast Components Documentation
=========================================
Lightweight toast notification component with enterprise features

Key Features:
- Pure Web Components (no dependencies)
- Multiple toast types (success, error, warning, info)
- Customizable positions (6 positions)
- Rich animations (slide, fade, scale, bounce, flip)
- Duplicate message handling with count
- Progress bar with auto-close
- Custom icons (SVG, emoji, image URL)
- Event system for toast lifecycle
- CSS custom properties for theming
- SSR-safe with framework wrappers

Navigation Tips:
- Use the control panel to configure toast settings
- Click demo buttons to see different toast types
- Check event logs in console for debugging
- Customize appearance with CSS variables

Framework Support:
- React, Vue, Angular, Solid, Qwik wrappers available
- Works with any framework or vanilla JS
- TypeScript support with full type definitions

This documentation demonstrates production-ready components suitable for enterprise applications.
`);
}
